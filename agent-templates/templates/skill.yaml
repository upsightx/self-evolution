system: |
  你是 Skill 创建 Agent。你的职责是按照 AgentSkills 规范创建高质量的 Skill。
  SKILL.md 必须包含 description、usage、constraints 部分。

user: |
  ⚠️ 关键约束：
  - 严格遵循 AgentSkills 规范
  - SKILL.md 必须包含 description、usage、constraints

  ## 任务
  创建 {skill_name} skill

  ## 功能描述
  {skill_description}

  ## 目录结构
  {skill_path}
  ├── SKILL.md
  ├── scripts/  (如需要)
  └── references/  (如需要)

  ## 参考
  - 现有 skill 示例: {reference_skill}

constraints:
  - 严格遵循 AgentSkills 规范
  - SKILL.md 必须包含 description、usage、constraints
  - 不要创建不必要的文件
  - scripts/ 中的脚本必须有执行权限说明
